O que é gitignore?
Evita que certos tipos de arquivos, dados sensíveis ou arquivos gerados automaticamente sejam adicionados ao repositório Git

//Visualiza as origins
git remote -v

//Remove todas as origens
git remote rm origin

//Mover ou renomear um arquivo diretamente do git
git mv pastaRemetente pastaDestinatária

//Apagar branch
git branch -d <nome>

//Mudar de branch
git checkout <nome>

//Criar e mudar de branch
git checkout -b <nome>

//Salvar alterações do código em area oculta, e reverter o diretório ao estado do último commit
git slash

//Lista todos os stashes disponíveis
git stash list

//Aplica as alterações de um stash específico ao diretório de trabalho atual
git stash apply n, onde n é o número do stash

//Visualiza alterações nos testes stash
git stash show -p n

//Apagar uma stash
git stash drop n

//Apagar todos os stash
git stash clear

//Puxar todos os branchs remotos
git fetch -a

//Adicionar submodulos
git submodulo link do diretório remoto

//Ver as diferenças de commits das branchs
git diff

//Mostra todos os commit feito no projeto
git shortlog

//Remove os arquivos que não usaram o comando add
git clean ou -f pra forçar a ação

//Verificar se está tudo certo no diretório
git fsck

//Verifica as mudanças no diretório
git reflog

//Reorganizar ou rebasear a história de commits de uma branch
git rebase branch_destino - Rebasing em relação a uma branch
git rebase -i commit_referencia - Interativamente reorganizando commits

pick - levar todos os commits
squash - unir a mensagem ao último commit
reword - modificar a mensagem
:x! - arquivo seja salvo
